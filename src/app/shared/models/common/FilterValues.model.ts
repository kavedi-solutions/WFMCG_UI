export interface FilterValues {           // Used for Filter Source & Filter Selection
  title: string;                          // title to send to api call
  value?: string;          // value of selected filter
}
